### Bandit config file generated by Bandit
skips: []

### (optional) list included test IDs here:
tests: ['B101', 'B102', 'B103', 'B104', 'B105', 'B106', 'B107', 'B108', 'B110', 'B112']

### (optional) list skipped test IDs here:
exclude_dirs: [
  'tests',
  'test',
  '.git',
  '.github',
  'venv',
  'env'
]

### profiles may be used to include sets of tests
profiles:
  medium:
    include:
      - B101  # assert_used
      - B102  # exec_used
      - B301  # pickle
      - B324  # hashlib
    exclude:
      - B110  # try_except_pass
      - B404  # import_subprocess
      - B603  # subprocess_without_shell_equals_true
      - B607  # start_process_with_partial_path
